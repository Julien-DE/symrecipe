{% extends "base.html.twig" %}

{% block title %}
	SymRecipe - Accueil

{% endblock %}

{% block body %}

	<div class="container mt-4">
		<div class="jumbotron">
			<h1 class="display-4">Bienvenue sur SymRecipe</h1>
			<p class="lead">SymRecipe est une application qui va te permettre de créer des recette à base d'ingrédients que tu auras toi-même créer. Tu pourra partager tes recettes à la communauté du site, ou bien les garder en privée.</p>
			<hr class="my-4">
		</div>
		<div class="recipes">
			<h2 class="mt-4">
				Recette de la communauté</h2>
			<div class="d-flex justify-content-center flex-wrap">
				{% for recipe in recipes %}
					<div class="card text-white bg-info mb-3 mx-3" style=" max-width: 20rem;">
						<div class="card-header">Recette n°
							{{ recipe.id}}</div>
						<div class="card-body">
							<h4 class="card-title">{{recipe.name}}</h4>
							<p class="card-text">{{recipe.description|slice(0,100) ~ '...' }}</p>
						</div>
					</div>
				{% endfor %}
			</div>
			<hr class="my-4">
		</div>

		<div>
			<p>Pour rejoindre la communauté rendez-vous sur la page d'inscription.</p>
			<a class="btn btn-primary btn-lg" href="#" role="button">Inscription</a>
		</div>


	</div>
{% endblock %}
